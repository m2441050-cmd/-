name: Morning Todo Report

on:
  schedule:
    - cron: "0 18 * * *"  # JST 3:00（GitHubはUTCなので18時）
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Call Supabase Edge Function
        run: |
          curl -X POST https://eycneuftjdooykzjbioy.supabase.co/functions/v1/todo-morning-report
