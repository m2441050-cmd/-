name: Todo Morning Report (debug)

on:
  schedule:
    - cron: "0 18 * * *"   # JST 3:00
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Call Supabase Edge Function (debug)
        run: |
          echo "=== calling todo-morning-report ==="
          curl -i -X POST https://eycneuftjdooykzjbioy.supabase.co/functions/v1/todo-morning-report
          echo ""
          echo "=== done ==="
